<form method="post" action="{{ path('app_tier_list_delete', {'id': tier_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this tier list?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tier_list.id) }}">
    <button class="btn">Delete</button>
</form>
